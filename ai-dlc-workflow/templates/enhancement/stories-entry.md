# Story entry (draft) — <change-name>

Drafted during the **plan** step — Plan Mode, before any code is written — in the same format as
`/specs/baseline/stories.md`. This is what the plan commits to delivering, not a stub, and not a
write-up invented after the fact. Stop here and get explicit review/confirmation of the story before
implementation starts.

After implementation and verification, confirm this entry still matches what was actually built
(correct it if implementation diverged from the approved plan), then append it to
`/specs/baseline/stories.md` as part of the same change — not a follow-up.

---

As a [role], I can [capability], so that [benefit].

**Status:** In Progress _(set at plan-time approval; change to Done once implemented, verified, and committed to `/specs/baseline/stories.md` — step 5 of the spec-first workflow)_

**Acceptance criteria:**
- [Criterion 1 — drawn from requirements.md]
- [Criterion 2]
- [...]

**Source:** `/specs/<change-name>/` (archival once `Done` — see `requirements.md` for the Jira story/epic reference, if any)
